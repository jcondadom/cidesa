
all:
  propel:
    class:          sfPropelDatabase
    param:
      phptype:            pgsql
      hostspec:           localhost
      database:           SIMA
      username:           postgres
      password:           postgres
      schema:             SIMA017
  sima_user:
    class:          sfPropelDatabase
    param:
      phptype:            pgsql
      hostspec:           localhost
      database:           SIMA
      username:           postgres
      password:           postgres
      schema:             SIMA_USER
